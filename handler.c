/* handler.c - responsible for parsing signals from the IRC server. */

/* Standard headers. */
#include <stdlib.h>
#include <stdio.h>
#include <string.h>

/* Handler required headers. */
#include <time.h>

/* Local Headers. */
#include "handler.h"
#include "command.h"
